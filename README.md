# Proyecto1-SO

Estructura de directorios
src -> Archivos fuente, es decir, archivos .c
obj -> Archivos objetos (.o) y archivos .d
include -> Archivos de cabeceras, es decir, archivos .h

Ubicación del binario generado con Makefile -> Directorio raiz

Makefile soporta make, make clean y make all
